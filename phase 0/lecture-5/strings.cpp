#include <iostream>
#include <string.h>
using namespace std;

int main() {
    // char name1[] = "World";
    char name2[] = {'W', 'o', 'r', 'l', 'd', '\0'}; // notice the null char, it means the string is ending there
    char name3[6] = {'W', 'o', 'r', 'l', 'd', '\0'};
    // cout << "name1 = " << name1 << '\n';
    // cout << "name2 = " << name2 << " " << strlen(name2) << " " << name2[4] << " " << name2[5] << "stop" << '\n';
    // cout << "name3 = " << name3 << " " << strlen(name3) << '\n';

    // char name[100];
    // cin >> name;
    // cout << name << '\n';

    // int x[6] = {19, 10, 8, 17, 9, 15};
    // cout << x << '\n';

    // reverse a string
    
    // char name[100];
    // cin.get(name, 8);
    // cout << name << '\n';

    // char arr[5] = "hello";
    // cout << arr;

    // char text[100];
    // cin.get(text,7);
    // int len = strlen(text);
    // // strrev(text);
    // // cout << text[2];

    // // cout << len << " " << text << "\n";
    // char reverse[len];
    // for(int i = 0 ; i < len ; i++){
    //     reverse[i] = text[len - i - 1];
    // }

    // cout << reverse << " " << strlen(reverse) << "\n";

    // cout << reverse [5] << "s" << reverse[6] << "t" << reverse[4] << "\n"; 

    // cout << text << " " << len << " " << text << "\n";
    

    return 0;
}